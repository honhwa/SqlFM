// =====================================================================
// 注意：LexerTests 已暂时禁用。
// 原因：旧的自研 SqlLexer/SqlToken 已被 Core 库替代，
// Core 使用 ScriptDom tokenizer（CaseConverter）和 PoorMansTSQLFormatter，
// 不再暴露 SqlLexer/SqlToken/SqlParser 等 API。
// 待 Core 库提供对应的测试接口后，将用 Core 的 API 重写这些测试。
// =====================================================================
