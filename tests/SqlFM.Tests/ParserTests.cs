// =====================================================================
// 注意：ParserTests 已暂时禁用。
// 原因：旧的自研 SqlParser/SqlNode/SqlNodeType 已被 Core 库替代，
// Core 使用 ScriptDom parser 和 PoorMansTSQLFormatter，
// 不再暴露 SqlParser/SqlNode 等 API。
// 待 Core 库提供对应的测试接口后，将用 Core 的 API 重写这些测试。
// =====================================================================
