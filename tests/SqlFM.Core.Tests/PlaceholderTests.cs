using Xunit;

namespace SqlFM.Core.Tests
{
    /// <summary>
    /// 占位测试：验证 Core 测试项目可正常编译和运行。
    /// 后续将替换为实际的 Core 库单元测试。
    /// </summary>
    public class PlaceholderTests
    {
        [Fact]
        public void CoreProjectBuilds()
        {
            Assert.True(true);
        }
    }
}
