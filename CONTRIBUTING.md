# 开发规范（贡献约定）

> SqlFM 项目开发规则约束。所有参与本仓库代码贡献的开发人员均需遵守。
> 规范目标：注释可维护、提交可追溯、架构边界清晰。

---

## 一、代码注释规范（核心红线）

### 1.1 适用范围

以下四类源码**必须**带合规中文注释：

| 类型 | 说明 |
|------|------|
| **C#** | 全部 `.cs` 业务逻辑、引擎、命令、服务代码 |
| **XAML** | WPF 窗口 / 控件的结构与关键绑定说明 |
| **T-SQL** | 存储过程、脚本、批量处理中的 SQL |
| **单元测试** | 测试用例的意图与覆盖边界 |

### 1.2 注释纪律（不可逾越）

新增 / 重构注释时，严格遵守以下红线——**只动注释，不动逻辑**：

1. **仅新增注释**：所有修改均为添加 `///` XML 文档注释、行内 `//` 或块注释。
2. **不改动业务逻辑**：不调整方法体、条件分支、循环结构、SQL 查询、异常处理。
3. **不重构代码**：不重命名变量 / 方法 / 类，不调整方法签名，不改变访问修饰符。
4. **不调整文件结构**：不移动、拆分或合并任何文件。
5. **不新增 / 删除引用**：不增删 `using` 指令、NuGet 包或项目引用。

> 例外：单独的"功能开发 / Bug 修复"提交可正常改动逻辑，但本红线针对**纯注释补充**类改动。

### 1.3 XML 文档注释要求

公开 API 一律使用 `///` 文档注释，标准如下：

| 目标 | 要求 |
|------|------|
| 类 / 接口 / 枚举头部 | `<summary>` 说明所属业务模块与核心作用 |
| 公开方法 | 完整 `<summary>` + `<param>` + `<returns>` + `<exception>` |
| 私有方法 | `<summary>` 简要说明职责 |
| 属性 | `<summary>` 说明业务含义和默认值 |
| 常量 / 配置项 | `<summary>` 说明取值用途 |
| 枚举值 | `<summary>` 说明每个枚举成员含义 |
| 内部类 / Visitor | `<summary>` 说明收集目标和访问逻辑 |

### 1.4 语言

**所有注释使用简体中文。** 术语（如 SQL、AST、Pipeline）可保留英文，不必翻译。

---

## 二、提交信息规范

统一使用**中文 + Conventional Commits** 风格：

```
<type>(<scope>): <中文简述>
```

### 2.1 type 取值

| type | 含义 |
|------|------|
| `feat` | 新功能 |
| `fix` | 缺陷修复 |
| `docs` | 文档变更 |
| `refactor` | 重构（不改外部行为） |
| `test` | 测试新增 / 修改 |
| `chore` | 构建 / 脚本 / 工具链 |
| `style` | 格式化 / 注释（不改逻辑） |

### 2.2 scope 取值（参考）

`vsix`（扩展主项目）、`core`（格式化引擎）、`cli`（命令行）、`setup`（安装包）、`ui`（配置窗口）、`vsct`（命令表）、`test`（测试）。

### 2.3 示例

```
fix(vsix): 修复 SSMS 22 扩展子菜单不显示的已知 Bug
feat(core): 新增 CTE 子查询关键字右对齐支持
style(core): 补全 FormatterPipeline 私有方法 XML 注释
```

---

## 三、分支与 PR 约定

- **分支命名**：功能用 `feat/xxx`，修复用 `fix/xxx`。
- **PR 流程**：Fork → 分支开发 → 补充测试 → 确保 `dotnet test` 通过 → 提交 PR。
- **报告 Bug**：请包含 SSMS 版本、SqlFM 版本、最小复现 SQL、期望 vs 实际输出。

---

## 四、架构边界约束

1. **`SqlFM.Core` 不得依赖 VS / WPF**。核心格式化逻辑（引擎、后处理、预设）必须可在无 VS 环境下独立运行（CLI、测试均依赖此约束）。
2. **新增格式化规则**：必须在 `FormatterTests.cs`（或对应测试文件）覆盖正向 + 反向用例，CI 的 `dotnet test` 不通过不得合并。
3. **后处理器 / 重构工具接入**：统一经由 `FormatterPipeline` 的 6 步管道接入（豁免提取 → 预重构 → 主格式化 → 后处理 → 恢复豁免 → 最终清理），禁止在管道外硬编码插入格式化逻辑。

---

*本规范固化自项目长期实践，随架构演进可经 PR 修订。*
